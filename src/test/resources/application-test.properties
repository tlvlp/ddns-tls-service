# DNS records
records.file.path = ${RECORDS_FILE_PATH:deployment/ddns_records/ddns_records.json}
record.update.schedule = ${RECORD_UPDATE_SCHEDULE:* 10 * * * *}

# DDNS Service
ddns.service.active = ${DDNS_SERVICE_ACTIVE:true}
ip.check.schedule = ${IP_CHECK_SCHEDULE:* 10 * * * *}
external.ip.apis.csv = ${EXTERNAL_IP_API_CSV:http://checkip.amazonaws.com,https://myexternalip.com/raw}